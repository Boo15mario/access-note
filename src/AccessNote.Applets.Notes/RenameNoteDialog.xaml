<Window x:Class="AccessNote.RenameNoteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Note"
        Width="640"
        Height="240"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="OnLoaded"
        PreviewKeyDown="OnPreviewKeyDown">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               Text="Rename note"
               FontSize="28"
               FontWeight="Bold"
               AutomationProperties.Name="Rename note header" />

    <TextBlock Grid.Row="1"
               Margin="0,14,0,8"
               Text="Name:"
               FontSize="20"
               AutomationProperties.Name="Name label" />

    <TextBox x:Name="NameTextBox"
             Grid.Row="2"
             MinHeight="46"
             FontSize="22"
             AutomationProperties.Name="Note name" />

    <StackPanel Grid.Row="3"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,16,0,0">
      <Button x:Name="OkButton"
              Content="OK"
              Width="120"
              Height="48"
              Margin="0,0,10,0"
              FontSize="20"
              IsDefault="True"
              Click="OnOkClick"
              AutomationProperties.Name="Confirm rename" />
      <Button x:Name="CancelButton"
              Content="Cancel"
              Width="120"
              Height="48"
              FontSize="20"
              IsCancel="True"
              Click="OnCancelClick"
              AutomationProperties.Name="Cancel rename" />
    </StackPanel>
  </Grid>
</Window>
